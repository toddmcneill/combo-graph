xid: string @index(exact) .
name: string @index(term) .
uses: [uid] @reverse .
centrality: float @index(float) .

type Combo {
  xid
  name
  uses
}

type Card {
  xid
  name
  <~uses>
}

type Node {
  centrality
}
